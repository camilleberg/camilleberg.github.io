source 'https://rubygems.org'

group :jekyll_plugins do
    gem 'github-pages'
    gem 'jekyll-feed'
    gem 'jekyll-gist'
    gem 'jekyll-seo-tag'
    gem 'jemoji'
end

gem "webrick"
gem "csv"      # ← new: Ruby 4.0 removed this from stdlib
gem "base64"   # ← new: also removed, needed by jekyll-3.9 internally
gem "bigdecimal"